@model Education


<div class="accordion accordion-flush" id="accordionFlushEdu-@Model.Id">
    <div class="accordion-item">
        <div class="accordion-header" id="flush-heading-Edu-@Model.Id">
            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#flush-collapse-Edu-@Model.Id" aria-expanded="false" aria-controls="flush-collapse-Edu-@Model.Id">
                <div class="resume-content row">
                    <div class="resume-logo col-md-4">
                        <img src="~/images/@Model.ImageFileName" />
                    </div>
                    <div class="resume-info col-md-8">
                        <p class="title">@Model.Title</p>
                        <p class="location-time">@Model.City, @Model.Province | @Model.FromDate.ToString("MMM yyyy") – @(Model.ToDate.ToString("MMM yyyy") == DateTime.Now.ToString("MMM yyyy") ? "Present" : Model.ToDate.ToString("MMM yyyy"))</p>
                    </div>
                </div>
            </button>
        </div>
        <div id="flush-collapse-Edu-@Model.Id" class="accordion-collapse collapse" aria-labelledby="flush-heading-Edu-@Model.Id" data-bs-parent="#accordionFlushEdu-@Model.Id">
            @if (Model.Descriptions.Count > 0)
            {
                <div class="accordion-body">
                    <ul>
                        @foreach (string description in Model.Descriptions)
                        {
                            <li>@description</li>
                        }
                    </ul>
                </div>
            }
        </div>
    </div>
</div>
